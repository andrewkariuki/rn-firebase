import { StyleSheet } from "react-native";
export const style = (fontSize: any) =>
  StyleSheet.create({
    heading: {
      fontSize: fontSize,
      marginBottom: 18,
      color: "#06040A",
      fontWeight: "bold",
    },
  });
